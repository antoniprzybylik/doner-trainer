## Examples of how to use Doner Trainer

This directory contains a number of
examples which demonstrate usage of
neural network trainers present in
the `libdt` crate.

All examples can be executed with:

```
cargo run --example $name
```
